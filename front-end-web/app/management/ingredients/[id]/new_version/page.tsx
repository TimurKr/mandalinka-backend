export default function NewVersion() {
  return (
    <div>
      Tu sa bude vyrabať nová verzia
      {/* <NewVersionForm
        submit_url={`${process.env.CLIENT_API_URL}/management/ingredients/${params.id}/versions/`}
        options={options}
      /> */}
    </div>
  );
}
