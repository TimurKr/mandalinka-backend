{
  "configurations": [
    {
      "name": "Docker Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/web-front-end/package.json",
        "localRoot": "${workspaceFolder}/web-front-end"
      }
    }
  ]
}
